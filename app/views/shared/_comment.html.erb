<div class="panel panel-default" id= "comment-<%=comment.id%>">
    <div class="panel-heading">
      <class = "panel-title"><%= comment.user.email %> - <%= comment.created_at.strftime("%d/%m/%Y às %H:%M") %></class>
    </div>
    <div class="panel-body">
      <%= comment.description %>
    </div>
</div>
